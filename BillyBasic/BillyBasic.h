#pragma once

#include <string>
#include <vector>

void print(std::vector<std::string>);
std::vector<std::string> arguments(std::string);
void interpret(char*);
void read();
int main();
void BillyBasic();
